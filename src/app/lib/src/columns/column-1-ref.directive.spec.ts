import { Column1RefDirective } from './column-1-ref.directive';

describe('Column1RefDirective', () => {
  it('should create an instance', () => {
    const directive = new Column1RefDirective();
    expect(directive).toBeTruthy();
  });
});
