import { Column2RefDirective } from './column-2-ref.directive';

describe('Column2RefDirective', () => {
  it('should create an instance', () => {
    const directive = new Column2RefDirective();
    expect(directive).toBeTruthy();
  });
});
