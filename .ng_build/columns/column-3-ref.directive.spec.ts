import { Column3RefDirective } from './column-3-ref.directive';

describe('Column3RefDirective', () => {
  it('should create an instance', () => {
    const directive = new Column3RefDirective();
    expect(directive).toBeTruthy();
  });
});
